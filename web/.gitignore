# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dev-server.*.log

# env files — secrets stay in `.env` only (commit `.env.example` as a template)
.env
.env*.local
.env.development
.env.production
.env.test
.env.example

firebase-service-account.json
*-firebase-adminsdk-*.json

# AI / editor agent config (local only)
AGENTS.md
CLAUDE.md
.cursor/
.cursorrules
*.cursorrules

# local sqlite artifacts (project uses MongoDB)
*.db
dev.db

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma
